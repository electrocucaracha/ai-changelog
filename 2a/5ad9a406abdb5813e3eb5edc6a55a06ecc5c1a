{"category": "Changed", "changelog_entry": "Stabilized changelog merging to strictly append newer release sections, preserving existing content and preventing accidental backfilling or overwriting of existing releases.", "summary": "The codebase has undergone significant changes to improve its functionality and security. The `subprocess` calls have been replaced with `GitPython` for git notes and tags, eliminating shell-out dependencies and improving error handling. Additionally, the changelog merge logic now appends only strictly newer release sections, preserving existing content and preventing accidental backfilling of old tags. The code also includes security improvements, such as using the Ollama HTTP API instead of the CLI for model pulling, and updates to the tests to cover new functionality.", "version": 1}

{"category": "Added", "changelog_entry": "Enabled persistent changelog entry rendering for finalization stage by precomputing and storing both detailed summaries and Keep a Changelog entries as versioned JSON payloads in each Git note.", "summary": "Changelog entries are now persisted as versioned JSON payloads in each Git note, including both the detailed summary and a precomputed Keep a Changelog\u2013style entry. This change enables final rendering to group and render changelog entries without additional model calls or diff hydration. The CLI now writes each note immediately after a worker completes, leaving durable checkpoints for interrupted runs and future processing of missing commits.", "version": 1}

{"category": "Changed", "changelog_entry": "Streamlined CI runs and reduced maintenance burden by limiting the tox configuration to only include Python 3.12 in the envlist, and ensured type checking accuracy by updating the mypy configuration to target Python 3.12 and clarifying missing import overrides for supported modules.", "summary": "Tox configuration was updated to only include Python 3.12 in the envlist, streamlining CI runs and reducing maintenance burden. The mypy configuration now targets Python 3.12, ensuring type checking aligns with the supported runtime. Type checking accuracy was improved by updating mypy overrides for missing imports, including click, headroom, and pytest modules, and clarifying patterns for litellm and git. This change assumes Python 3.12 is the only supported version going forward.", "version": 1}

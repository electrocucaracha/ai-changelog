{"category": "Fixed", "changelog_entry": "The Makefile now reliably installs textlint dependencies globally during formatting by prompting for a password on systems with sudo configured if necessary.", "summary": "The Makefile now uses `sudo` for global npm install of textlint dependencies to ensure reliable installation during formatting, addressing previous failures for users without write permissions. This change introduces no breaking behavior but may prompt for a password on systems with sudo configured. The fmt target's reliability is improved by this modification.", "version": 1}

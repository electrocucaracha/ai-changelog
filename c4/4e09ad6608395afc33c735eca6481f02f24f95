{"category": "Fixed", "changelog_entry": "Reliably formats code by ensuring textlint and its terminology rule are installed globally before running the formatter with the --fix flag.", "summary": "The Makefile's fmt target now reliably formats code by ensuring textlint and its terminology rule are installed globally, preventing inconsistent linting behavior due to missing dependencies. This change adds explicit checks for global installations of these tools before running textlint with the --fix flag. The invocation also switches from using npx to the globally installed textlint, resolving issues with local dependencies.", "version": 1}

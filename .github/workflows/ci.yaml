# SPDX-license-identifier: Apache-2.0
##############################################################################
# Copyright (c) 2026
# All rights reserved. This program and the accompanying materials
# are made available under the terms of the Apache License, Version 2.0
# which accompanies this distribution, and is available at
# http://www.apache.org/licenses/LICENSE-2.0
##############################################################################
name: Continuous Integration
# yamllint disable-line rule:truthy
on:
  push:
  pull_request:
permissions: read-all
jobs:
  unit-tests:
    name: Run unit tests
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Setup Python environment
        uses: ./.github/actions/setup-python-env
      - name: Run unit tests
        run: uvx tox -e py312
  # jscpd:ignore-start
  mutation-tests:
    name: Run mutation tests (${{ matrix.module }})
    runs-on: ubuntu-latest
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        module: [ai_provider, changelog, config, git_helper, main]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Setup Python environment
        uses: ./.github/actions/setup-python-env
      - name: Install test dependencies
        run: uv sync --extra test
      - name: Restrict mutations to ${{ matrix.module }}
        run: python ci/restrict_mutations.py --module "${{ matrix.module }}"
      - name: Run mutation tests
        env:
          PYTHONPATH: src
        run: uv run mutmut run
      - name: Export mutation stats
        if: always()
        env:
          PYTHONPATH: src
        run: uv run mutmut export-cicd-stats
      - name: Upload mutation stats
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: mutmut-stats-${{ matrix.module }}
          path: mutants/mutmut-cicd-stats.json
  mutation-gate:
    name: Enforce no surviving mutations
    runs-on: ubuntu-latest
    needs: mutation-tests
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download all mutation stats
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          pattern: mutmut-stats-*
          path: mutmut-stats
      - name: Check no surviving mutations
        run: python ci/check_mutation_gate.py --stats-dir mutmut-stats
        # jscpd:ignore-end

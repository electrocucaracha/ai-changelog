{"category": "Added", "changelog_entry": "Determined at runtime, the default model now adapts to Apple Silicon Macs, resolving to the quantized Llama 3.1 8B Instruct variant for improved latency and quality, while other systems continue to use the standard Llama 3.1 model.", "summary": "The configuration now supports adaptive model selection based on the runtime environment, with Apple Silicon Macs defaulting to the quantized Llama 3.1 8B Instruct variant for improved latency and quality. The default model is now determined at runtime and may differ from previous releases on Apple Silicon Macs. Additionally, the configuration now supports retry attempts and retry backoff seconds, configurable via environment variables, and includes an optional enable headroom flag.", "version": 1}

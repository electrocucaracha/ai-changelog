{"category": "Changed", "changelog_entry": "Enabled GitHub Actions workflows to properly attribute commits and enabled parallel changelog generation, resulting in improved performance for larger repositories with no impact on release logic.", "summary": "GitHub Actions workflows now properly attribute commits and enable parallel changelog generation. The git user name and email are set to the standard GitHub Actions bot, ensuring commit acceptance. Additionally, the changelog generation step now uses two workers, improving performance for larger repositories. This change has no impact on release logic.", "version": 1}

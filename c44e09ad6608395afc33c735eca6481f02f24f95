{"category": "Fixed", "changelog_entry": "The Makefile's fmt target now reliably formats code across environments by ensuring the globally installed textlint and its terminology rule are available.", "summary": "The Makefile's fmt target now reliably formats code across environments by ensuring the globally installed textlint and its terminology rule are available. This change fixes inconsistent linting behavior caused by missing global dependencies. The invocation now uses the globally installed textlint instead of npx, avoiding issues with local dependencies.", "version": 1}

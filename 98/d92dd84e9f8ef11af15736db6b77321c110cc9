{"category": "Fixed", "changelog_entry": "Robustly restricts mutmut to target only the current module in the test matrix, preventing silent misconfiguration and improving maintainability.", "summary": "The workflow for running mutation tests now robustly restricts mutmut to target only the current module by updating or inserting the only_mutate setting in pyproject.toml. This change replaces a brittle sed approach with a Python script that ensures reliable updates, preventing silent misconfiguration. The change improves maintainability but may require further validation for edge cases in pyproject.toml formatting.", "version": 1}

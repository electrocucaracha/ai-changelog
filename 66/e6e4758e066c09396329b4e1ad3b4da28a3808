{"category": "Added", "changelog_entry": "Enabled users to monitor and optimize LLM resource consumption by tracking aggregate token usage for completions.", "summary": "Token usage tracking was added to LiteLLM completions for monitoring and optimization. A `TokenUsage` dataclass accumulates prompt, completion, and total token counts across all completion calls. This data is exposed via the `AIProvider` and printed after CLI runs, enabling users to track resource consumption. Tests and mocks were updated to support the new usage tracking attribute.", "version": 1}

{"category": "Removed", "changelog_entry": "Simplified the CI pipeline by eliminating the non-essential reporting feature that appended per-module mutation summaries to the GitHub Actions job summary, while still exporting and uploading mutation statistics as artifacts.", "summary": "The publish_mutation_summary.py script and its associated workflow step were removed from the CI pipeline to simplify maintenance and eliminate a non-essential reporting feature. This change reduces the pipeline's complexity and overhead, but has no impact on mutation testing or artifact availability. Mutation statistics are still exported and uploaded as artifacts, but the per-module summary is no longer appended to the GitHub Actions job summary.", "version": 1}

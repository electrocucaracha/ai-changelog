{"category": "Changed", "changelog_entry": "Stabilized CI run reliability by updating action dependencies to the latest patch versions, ensuring compatibility and stability with upstream changes.", "summary": "The GitHub workflows for documentation and linting have been updated to use the latest patch versions of action dependencies, specifically github-action-markdown-link-check and git-diff-action, to ensure compatibility and stability with upstream changes and reduce the risk of known issues in CI runs. This update does not introduce breaking behavior or API changes. The config schema remains unchanged, and no migration steps are required. The primary user outcome is improved CI run reliability.", "version": 1}

{"category": "Fixed", "changelog_entry": "The Makefile's fmt target now reliably installs textlint dependencies globally, resolving issues for users without write permissions, and may prompt for a password on systems with sudo configured.", "summary": "The Makefile's fmt target now uses sudo to install textlint dependencies globally, addressing a previous issue where users without write permissions would encounter errors. This change ensures the textlint installation completes as intended, improving the fmt target's reliability. The addition of sudo may prompt for a password on systems with sudo configured.", "version": 1}

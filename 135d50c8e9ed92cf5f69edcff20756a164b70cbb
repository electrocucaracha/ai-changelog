{"category": "Changed", "changelog_entry": "Documentation pages now use YAML front matter to define titles, navigation order, and parent-child relationships, enabling richer navigation menus and consistent page metadata.", "summary": "Documentation pages now use YAML front matter to define titles, navigation order, and parent-child relationships, enabling richer navigation menus and consistent page metadata. This change does not introduce breaking behavior or migration requirements. The API and CLI contracts remain unchanged. There is no security impact. The config schema has been updated to accommodate the new metadata. Users and maintainers can now expect improved navigation and organization in static site generators.", "version": 1}

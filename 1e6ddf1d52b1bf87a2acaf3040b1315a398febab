{"category": "Added", "changelog_entry": "Enabled users to add new LLM backends without modifying the upstream project by introducing a plugin system for custom LiteLLM providers via Python entry points.", "summary": "Custom LLM providers can now be added without modifying the upstream project. This is achieved through a plugin system that discovers and registers custom providers via Python entry points. Providers are loaded from installed packages and registered with LiteLLM's custom provider map before the first model call. Registration is idempotent and failures to load a provider are logged as warnings, ensuring that broken plugins do not block startup.", "version": 1}

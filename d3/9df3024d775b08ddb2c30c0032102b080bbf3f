{"category": "Fixed", "changelog_entry": "The GitHub workflow for updating dependencies now stabilizes its token usage by enabling a fallback to the GitHub token if the WORKFLOW_TOKEN secret is missing.", "summary": "The GitHub workflow for updating dependencies now uses a fallback token in the check-versions job, ensuring that the workflow can run even if the WORKFLOW_TOKEN secret is missing. This change affects the job's token usage, allowing it to fall back to the GitHub token if the secret is not available. The update does not introduce any breaking behavior or require migration steps.", "version": 1}

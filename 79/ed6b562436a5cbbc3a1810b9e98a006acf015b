{"category": "Added", "changelog_entry": "Hardened the Ollama model validation to ensure readiness before processing, causing the CLI to exit early if the model is missing or the API is unreachable and requiring downstream tools to adjust their workflows due to the breaking behavior.", "summary": "The Ollama model validation has been improved to ensure readiness before processing. The AI provider now checks for the configured Ollama model at startup and pulls it once if missing, preventing concurrent workers from racing and potentially failing. The CLI now fails fast with a clear error if the Ollama API is unreachable or the model cannot be made available, improving user feedback and reliability. This change may require downstream tools to adjust their workflows due to the breaking behavior of the CLI.", "version": 1}

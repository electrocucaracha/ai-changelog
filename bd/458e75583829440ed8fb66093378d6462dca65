Category: Changed

The regular expression used to check for the "Unreleased" heading was simplified by replacing it with a direct string comparison after stripping whitespace and converting to lowercase, improving readability and performance without altering behavior. This change affects no downstream logic and maintains strict adherence to the expected input format. The original implementation's complexity is reduced, but its functionality remains unchanged.

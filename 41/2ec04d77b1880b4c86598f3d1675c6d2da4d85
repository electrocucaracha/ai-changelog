{"category": "Changed", "changelog_entry": "Enabled the project's chosen toolchain by configuring yamllint and isort, and disabling pylint, ensuring consistent Python formatting with ruff and black.", "summary": "The project's super-linter CI failures were fixed by disabling pylint and ruff-format, adding yamllint and isort configurations, and modifying the linter.yml file to reflect the project's chosen toolchain. This change also requires actions/checkout to be run before using the local composite action. Additionally, the project's Python formatting style is now enforced by ruff and black, and pylint is disabled due to conflicts with the project's chosen style.", "version": 1}

{"category": "Added", "changelog_entry": "Improved changelog merging now replaces changed or removed release sections instead of appending them, ensuring idempotency and correcting stale entries in place, and existing changelog behavior is updated on rerun.", "summary": "Changelog merging now replaces changed or removed release sections, ensuring idempotency and correcting stale entries. This breaks existing changelog behavior, updating previously generated sections on rerun. Additionally, CLI flags for parallelism and retry behavior have been added, allowing users to tune worker count and retry settings via `--workers`, `--retry-attempts`, and `--retry-backoff-seconds`. Tests cover the new options and updated changelog merging behavior.", "version": 1}

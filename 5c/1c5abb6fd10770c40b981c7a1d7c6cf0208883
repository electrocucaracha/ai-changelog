{"category": "Changed", "changelog_entry": "Simplified CLI test setup by introducing reusable helpers that reduce duplication and improve maintainability, allowing test logic to focus on behavior rather than scaffolding, without affecting the CLI's functionality or output.", "summary": "The CLI test setup was refactored with reusable helpers to reduce duplication and improve maintainability. This change introduces helper functions for building commits, repositories, and patching dependencies, as well as helpers for installing a fake AI provider and invoking the CLI. Existing tests are rewritten to use these helpers, ensuring consistent test setup and focusing on behavior rather than scaffolding. No functional changes were made to the CLI or its output.", "version": 1}

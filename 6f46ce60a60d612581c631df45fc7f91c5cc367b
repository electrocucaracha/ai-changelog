{"category": "Added", "changelog_entry": "Enabled the final rendering stage to group and render changelog entries without additional model calls or diff hydration by persisting precomputed changelog entries and summaries as versioned git notes.", "summary": "Git notes now store both the summary and a precomputed changelog entry for each commit, enabling final rendering to group and render entries without additional model calls or diff hydration. This change includes backward-compatible note format updates, concurrent checkpointing behavior, and updated tests and documentation. The CLI now writes each note as soon as a worker completes, ensuring interrupted runs leave durable checkpoints and future runs only process missing commits.", "version": 1}
